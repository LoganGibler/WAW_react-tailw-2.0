apiVersion: v1
kind: Service
metadata:
  name: waw-frontend
  namespace: default
spec:
  selector:
    app: waw-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30082
  type: NodePort